fun main() = {
	println(1 + 2 * 2);
	println(g(f(1,2)));
}

fun f(a, b) = {
	return if (a > b)
		b
	else if a == b
		a + b
	else
		a;
}

fun g(x) = {
	var y;
	y = 0;
	while y > x
		y = y + 1;

	return y;
}
